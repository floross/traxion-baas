import { nestjsNestjsResolvers } from './nestjs-nestjs-resolvers';

describe('nestjsNestjsResolvers', () => {
  it('should work', () => {
    expect(nestjsNestjsResolvers()).toEqual('nestjs-nestjs-resolvers');
  });
});
